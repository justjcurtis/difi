const expectedMissing = ['this key is to test sorting', 'a','t', 'T', '^small', '$small', '^big', '$big', 'D', 'another missing key']
const expectedMissingIgnoreCase = ['this key is to test sorting', 'a', 't', 'T', '^small', '$small', '^big', '$big', 'another missing key']

module.exports = {
    expectedMissing,
    expectedMissingIgnoreCase
}